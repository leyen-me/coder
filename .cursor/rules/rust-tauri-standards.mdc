---
alwaysApply: true
---
# Rust + Tauri 规范

面向 Rust 2021 + Tauri 2。后端代码应像稳定系统组件一样设计：边界清晰、错误可诊断、平台差异显式、行为可测试。

## 架构与目录

- `main.rs` 只保留进程入口；`lib.rs` 负责组装 Tauri builder、插件、命令和启动流程。
- 按职责拆分 `src-tauri/src` 模块，如 `commands/`、`state/`、`window/`、`config/`、`domain/`；模块名使用领域词，不使用泛化的 `helpers` 容器。
- Tauri command 是前后端协议边界，只做参数校验、权限/状态协调和调用领域逻辑；复杂业务放到可单测的普通 Rust 函数。
- 前后端共享的数据结构集中定义，使用 `serde::{Serialize, Deserialize}`，字段命名和兼容策略必须稳定。
- 平台相关逻辑用 `#[cfg(...)]` 明确隔离；不要让 macOS/Windows/Linux 特例散落在通用流程中。

## 文件与模块边界

- 单个模块只表达一个抽象层级；文件过长、同时包含 Tauri 生命周期、业务计算和平台适配时，应拆分。
- 公共 API 使用 `pub(crate)` 作为默认可见性，只有跨 crate 需要时才使用 `pub`。
- 常量使用有领域含义的名称，避免魔法数字；涉及窗口尺寸、延迟、路径、权限时写清楚单位和依据。
- 文档注释用于解释公共函数的契约、失败语义和平台限制；普通注释解释“为什么”，不要复述“做什么”。

## 错误处理与日志

- 库内函数优先返回 `Result<T, E>`，不要在可恢复路径使用 `unwrap`/`expect`；入口级不可恢复错误可以 `expect`，但消息必须可诊断。
- Tauri command 返回给前端的错误要可序列化、可展示；不要把内部错误、路径、token 或敏感上下文直接暴露给 UI。
- 日志使用明确 target，如 `coder::window`；日志消息包含动作和失败原因，避免只打印 `error: {}`。
- 对启动、窗口、文件系统、外部进程等易受环境影响的操作，错误路径应可观察且不中断非关键流程。

## Tauri 设计规范

- 所有 `invoke_handler` 注册集中管理，命令命名稳定；前端调用应通过 TypeScript 封装层统一访问。
- `setup` 中只做启动期必须完成的工作；耗时任务应异步化或延后，避免阻塞首屏。
- 窗口操作优先封装在专门模块中，明确 label、尺寸单位、主线程要求和多窗口扩展方式。
- 能力、权限、插件和 CSP 变更必须最小化；新增权限要能说明用户价值和风险。
- 配置文件 `tauri.conf.json` 保持声明式；动态行为放在 Rust 模块中，不把复杂逻辑塞进配置。

## Rust 代码规范

- 代码优先表达不变量：使用类型、枚举、newtype 和小函数限制非法状态，而不是到处写防御式分支。
- 所有权和生命周期保持简单；除非有明确性能证据，不为避免 clone 引入复杂借用结构。
- 迭代器链以可读性为准；复杂转换拆成命名步骤或纯函数。
- 异步代码要明确取消、安全退出和共享状态策略；共享可变状态必须说明锁粒度，避免长时间持锁跨 await。
- 依赖新增要谨慎，优先使用标准库、Tauri 官方插件和已存在依赖；新增 crate 需有清晰维护收益。

## 测试与质量门槛

- 纯计算、路径转换、窗口布局、序列化协议和错误映射应优先写单元测试。
- Tauri 集成行为难以自动化时，至少把核心逻辑拆成普通函数测试，把薄 Tauri 层保留为适配层。
- 修改 Rust 代码后运行 `cargo fmt`、`cargo clippy` 和相关 `cargo test`；跨前后端协议变化还要运行 `pnpm build`。
- Review 自查：错误是否可诊断、平台差异是否隔离、命令协议是否稳定、权限是否最小、测试是否覆盖关键不变量。

## AI 修改原则

- 先读 `Cargo.toml`、`tauri.conf.json` 和相关模块，再决定新增依赖或目录。
- 不为了“更通用”提前引入框架式分层；当第二个真实用例出现时再抽象。
- 保持前后端协议显式：新增 command 时同步考虑 TypeScript 封装、类型、错误展示和测试。
- 不掩盖编译器警告，不使用 `allow`、`unwrap` 或大范围 `clone` 逃避设计问题。
