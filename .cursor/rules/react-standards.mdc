---
alwaysApply: true
---
# React 规范

面向 React 19 + TypeScript + Vite。写代码时优先保持简单、显式、可测试；让组件边界、数据流和副作用一眼可见。

## 架构与目录

- 按功能域组织代码，优先使用 `features/<domain>/`、`components/`、`hooks/`、`lib/`、`types/` 等稳定边界；不要把所有 UI、状态和 API 调用堆进单个文件。
- 页面或功能入口负责组合，通用组件只表达 UI 和交互契约，不直接了解业务流程。
- 与 Tauri/Rust 通信的代码集中封装在 `lib/tauri` 或功能域服务层，组件不要散落调用 `invoke`。
- 共享工具必须足够通用且有明确调用方；不要为了“一次可能复用”提前抽象。
- 文件和目录命名使用清晰领域词，避免 `utils.ts` 无限制膨胀；工具文件按能力命名，如 `format-date.ts`、`tauri-client.ts`。

## 文件与组件边界

- 一个文件只承担一个主要职责；组件超过约 150 行或出现多个独立状态/副作用时，优先拆分为子组件、hook 或纯函数。
- 组件使用函数组件，导出方式保持一致；公共组件显式定义 props 类型，避免 `any` 和隐式对象形状。
- 组件 props 以使用者心智命名，避免泄露内部实现；布尔 props 使用正向语义，如 `disabled`、`open`。
- 本地状态靠近使用处；跨组件状态先考虑提升到最近公共父级，再考虑 context 或状态库。
- 自定义 hook 只封装可复用的状态逻辑或副作用，不返回含糊的大对象；返回值命名稳定，便于测试和重构。

## 代码规范

- TypeScript 类型表达真实约束：优先使用精确类型、判别联合、`unknown` 后收窄；禁止用 `any` 绕过设计问题。
- 渲染逻辑保持声明式；复杂条件先提取为命名变量或小组件，避免 JSX 中嵌套过深的三元表达式。
- 副作用必须放在明确边界中，`useEffect` 只同步外部系统；不要用 effect 派生可由 props/state 直接计算的值。
- 事件处理函数命名体现意图，如 `handleSubmit`、`handleNameChange`；异步处理要显式管理 loading、error 与取消/过期响应。
- 列表渲染使用稳定业务 key，不使用数组 index 作为可变列表 key。
- 样式优先保持局部、可预测；class 命名表达结构与状态，避免依赖脆弱的全局级联。

## 可访问性与用户体验

- 表单控件必须有可访问名称，优先使用 `label`、`aria-label` 或明确的文本关系。
- 可点击元素使用语义化 `button`/`a`，不要用 `div` 模拟交互；键盘、焦点和 disabled 状态要完整。
- 用户可见错误应给出可恢复信息；开发日志不能替代 UI 反馈。
- 外部链接使用 `rel="noreferrer"`，需要新窗口时才加 `target="_blank"`。

## 质量门槛

- 修改共享组件、hook、数据转换或 Tauri 调用封装时，应补充单元测试或集成测试；窄小 UI 改动至少手动验证关键路径。
- 提交前运行相关检查：`pnpm build`，必要时运行测试和格式化。
- 不引入未使用依赖、隐式全局变量、死代码或无解释的大型重构。
- Review 自查：命名是否清楚、边界是否稳定、错误路径是否可见、未来新增同类功能是否容易落位。

## AI 修改原则

- 先观察现有文件风格，再最小化修改；不要顺手重排无关代码。
- 如果需要新增目录，先确保它代表稳定边界，而不是临时分类。
- 遇到类型错误时修正模型和边界，不用类型断言掩盖真实不确定性。
- 生成代码要能被人维护：少魔法、少隐式约定、少跨层直连。
