---
description: UI 设计规范，约束桌面端布局、视觉语言、组件用法与 Agent 交互体验
globs: src/**/*.{tsx,css}
alwaysApply: false
---

# UI 设计规范

面向 `Tauri 2 + React + shadcn/ui + Tailwind CSS`。目标不是做“炫酷界面”，而是做一个清晰、克制、可信赖的桌面 Agent 工作台。

## 核心原则

- 轻量优先：避免装饰性设计，优先让用户一眼看懂当前页面能做什么。
- 状态可信：任务状态、执行进度、错误原因、下一步动作必须可见，不能让用户猜。
- 可恢复：空状态、失败状态、权限不足、网络问题都要给出明确可执行的下一步。
- 内容优先：减少视觉噪音，突出任务、消息、文件、结果本身，而不是组件花样。
- 一致优先：同类页面使用相同布局、相同间距、相同交互模式，避免每个页面都“重新设计”。

## 产品语境

- 产品定位：小而美的 Desktop Agent，帮助用户把电脑上的复杂任务变成可交付结果。
- 核心场景：对话式任务台、文件工作台、研究整理助手。
- 设计目标：像 `VS Code` 一样结构清楚，像 `Notion/Linear` 一样简洁克制。

## 页面 Shell

- 应用默认采用 `顶部 Header + 左中右三栏主体` 的桌面布局。
- 左侧为会话与配置入口，中间为主 Chat 工作区，右侧为浏览器、编辑器、终端、Git 等辅助工作台。
- 右侧工作台默认折叠，仅在用户主动展开时出现，避免在有限窗口宽度里挤压主对话区。

```txt
+----------------------------------------------------------------------------+
| Header: 折叠 | 搜索 | ...                           右侧面板 | 任务多开      |
+----------------------------------------------------------------------------+
| LeftSidebar              | MainChatWorkspace              | RightWorkbench |
|--------------------------|--------------------------------|----------------|
| + 新建 Chat              | Chat A / Chat B / Chat C       | Browser        |
| Chat 历史                | 用户提问 / AI 回答 / 交互区    | Editor         |
| 设置 / 配置入口          | 当前活跃 Chat 的主工作流       | Terminal       |
|                          |                                | Git            |
+----------------------------------------------------------------------------+
```

### Header

- Header 保持较矮，优先作为全局控制条，而不是内容展示区。
- Header 左侧放全局高频动作，如侧栏折叠、全局搜索。
- Header 右侧放布局相关控制，如右侧工作台开关、任务多开面板入口。
- Header 不承载冗长标题、说明文字或复杂表单。

### 左侧栏

- 左侧栏是会话与配置导航，不是工作台。
- 左侧核心内容固定为：`新建 Chat`、`Chat 历史`、`设置/配置入口`。
- `新建 Chat` 是高频动作，位置应稳定且易于点击。
- `Chat 历史` 支持快速切换、重命名、继续对话，但不要塞入过多 hover 才能发现的操作。
- `设置/配置入口` 应清晰可达，但视觉优先级低于新建与历史。

### 中间主区

- 中间区域是产品主舞台，承载用户提问、AI 回答和持续人机协作。
- 主区必须围绕当前活跃 Chat 构建，确保阅读与输入连续。
- Chat 区域支持多开任务，用户可以同时打开多个 Chat 并在其间切换。
- 多开 Chat 的切换体验要像工作标签，而不是把多个任务内容混在同一滚动流中。

### 右侧工作台

- 右侧工作台包含 `Browser`、`Editor`、`Terminal`、`Git` 四类能力。
- 右侧默认折叠，展开后应允许切换不同工具视图。
- 右侧是辅助执行区，不应长期挤压中间主 Chat 的主要阅读宽度。
- 当右侧关闭时，中间区域应自然扩展，不保留无意义空白。

- `LeftSidebar` 建议固定宽度 `220px - 260px`，支持折叠。
- `Header` 建议高度控制在 `44px - 52px`。
- 中间 Chat 区优先占据剩余空间，是默认视觉焦点。
- 右侧工作台展开宽度建议 `320px - 420px`，并支持用户调整。

## 布局与间距

- 使用统一 4pt/8pt 间距体系，常用步进为 `4 / 8 / 12 / 16 / 24 / 32`。
- 页面外边距优先使用 `16` 或 `24`，卡片内边距优先使用 `16`。
- 同层级模块间距保持一致，不要一处 `12` 一处 `28`。
- 栅格与对齐优先保证阅读节奏，不为“填满空间”强行拉伸组件。
- 表单元素垂直堆叠时优先使用 `8` 或 `12` 间距；大区块之间优先使用 `16` 或 `24`。

## 视觉语言

- 布局风格参考 `VS Code`：分区明确、导航稳定、操作入口固定。
- 视觉风格参考 `Notion/Linear`：留白充足、边框柔和、颜色克制、层次自然。
- 默认避免高饱和大面积纯色背景，优先使用中性黑白灰，彩色仅用于状态语义（成功、警告、失败）。
- 阴影只用于表达浮层和层级，不用于“装饰”。
- 圆角保持温和统一，优先使用 `rounded-lg` 与 `rounded-xl`，不要同页混用多个极端圆角。

## Design Tokens

所有颜色、圆角、边框、前景色都应映射到 token，不要在业务组件里散落硬编码色值。

| Token | 用途 | Light | Dark |
| --- | --- | --- | --- |
| `--background` | 页面背景 | `#FAFAFA` | `#0F0F0F` |
| `--foreground` | 主文字 | `#171717` | `#FAFAFA` |
| `--card` | 卡片背景 | `#FFFFFF` | `#18181B` |
| `--muted` | 次级背景 | `#F4F4F5` | `#1A1A1A` |
| `--muted-foreground` | 次级文字 | `#71717A` | `#A1A1AA` |
| `--primary` | 主操作色 | `#171717` | `#FAFAFA` |
| `--primary-foreground` | 主按钮文字 | `#FFFFFF` | `#171717` |
| `--border` | 边框与分隔线 | `#E4E4E7` | `#27272A` |
| `--destructive` | 危险操作/失败 | `#DC2626` | `#EF4444` |
| `--warning` | 警告 | `#D97706` | `#F59E0B` |
| `--success` | 成功 | `#16A34A` | `#22C55E` |
| `--radius` | 基础圆角 | `0.5rem` | `0.5rem` |

- Tailwind 类优先使用语义 token，如 `bg-background`、`text-foreground`、`border-border`。
- 不要在业务页面直接写 `text-gray-500`、`bg-zinc-900` 这种绕过设计系统的样式。

## 字体与排版

- 正文字体优先使用系统 UI 字体栈，如 `-apple-system, Inter, sans-serif`。
- 代码、路径、命令、日志使用 `ui-monospace`。
- 常用字号层级：`12 / 14 / 16 / 20 / 24`。
- 正文默认以 `14` 或 `16` 为主，不要堆太多小字。
- 同一模块内标题层级清晰，避免“所有字都一样重”。
- 长段文本优先左对齐，不做居中排版。

## 组件层级

- 优先复用 `shadcn/ui` 基础组件，不重复造 `Button`、`Input`、`Dialog`、`Sheet`、`Tabs`、`Tooltip`。
- 基础 UI 组件放在 `src/components/ui/`。
- 带业务语义的组合组件放在 `src/components/` 或 `src/features/<domain>/components/`。
- 页面负责组合布局，不负责沉淀底层视觉规范。

## 基础组件规范

### Button

- 主按钮用于当前区域唯一主要动作。
- 同一区域最多一个主按钮，避免多个视觉竞争的 CTA。
- 工具栏、次级操作优先使用 `ghost` 或 `secondary`。
- 破坏性动作使用 `destructive`，如停止任务、删除、清空。

```tsx
// ❌ BAD：同一块区域出现多个主按钮
<Button>保存</Button>
<Button>删除</Button>

// ✅ GOOD：突出主动作，弱化次动作
<Button>保存</Button>
<Button variant="secondary">取消</Button>
<Button variant="destructive">删除</Button>
```

### Input / Textarea

- 输入框优先服务于任务表达与信息录入，不做多余装饰。
- 对话输入区是会话页的关键焦点，应提供足够宽度和明确提交方式。
- 长文本优先使用 `Textarea`，并支持自动增高或明确的多行输入体验。
- 占位文案用于提示内容类型，不要把完整说明文档塞进 placeholder。

### Card

- 卡片用于任务摘要、设置分组、结果摘要、信息块。
- 不要出现一层套一层的卡片地毯式堆叠。
- 卡片内部信息层级应明显，避免“所有内容都同等重要”。

### Badge

- Badge 只用于轻量状态标记，不承担长句说明。
- Agent 状态必须同时用颜色和文字表达，不能只靠颜色区分。

建议映射：

- `pending`: 中性灰
- `running`: 主色或中性强调
- `waiting_user`: 警告色
- `cancelling`: 橙色
- `cancelled`: 中性灰
- `completed`: 成功色
- `failed`: 失败色

### Dialog / Sheet / Popover

- 需要明确确认的危险操作使用 `Dialog`。
- 设置、详情、二级配置优先使用 `Sheet`。
- 小范围帮助说明、快捷操作菜单优先使用 `Popover` 或 `DropdownMenu`。
- 不要把复杂流程全部塞进弹层；超过一个主要任务流时应考虑独立页面。

### Tabs / Sidebar / Tree

- `Tabs` 适合切换同一对象的不同视图，不适合承载全局导航。
- 文件树、会话列表、任务列表应强调选中态、hover 态、焦点态。
- 树节点操作应在 hover 时出现，但核心信息不应被隐藏得太深。
- 多开的 Chat 应采用稳定可预期的标签式切换，不要把任务多开隐藏成难发现的二级入口。

## Agent 专用界面规范

Agent 是产品核心，因此相关页面优先强调过程可见性、状态连续性、可中断性。

### 会话页

- 结构建议为：Chat 标签区 + 消息流 + 输入区。
- 用户消息、Agent 结果、系统状态提示要有明确视觉区分。
- 输入区始终固定在用户心智稳定的位置，不随页面切换频繁漂移。
- 需要用户确认时，问题、选项、原因要集中展示，不要埋在日志里。
- 多开 Chat 时，同一时刻只突出一个活跃 Chat；其他 Chat 保留清晰但弱化的存在感。
- Chat 标签切换不应导致输入框、消息区、任务状态区位置来回变化。

### 任务执行页

- 必须展示当前状态、当前步骤、已完成步骤、当前工具、最近错误。
- 长任务优先使用步骤列表或时间线，而不是只显示一个 spinner。
- “停止任务”应始终可见，但不要误触；需要明确危险语义。
- 日志默认折叠展示，只在用户需要细节时展开。

### 文件工作台

- 文件树、预览区、编辑区职责清楚，避免多个区域都试图成为视觉中心。
- 文件路径、保存状态、修改状态要易于发现。
- 对二进制、未知格式、超长内容应有降级展示，而不是直接空白。

### Browser / Terminal / 面板区

- 面板区默认折叠，是辅助执行区，不应抢走主工作流焦点。
- 面板标题应清楚说明当前内容类型，如“终端输出”“网页预览”“任务详情”。
- 面板支持收起、切换和尺寸调整，默认记住用户上次偏好。
- 浏览器、编辑器、终端、Git 共享右侧工作台容器，不建议同时并排常驻展示。
- 工作台展开与收起入口放在 Header 右侧，避免用户在页面深处寻找。

## 交互状态

- 所有可点击元素都必须有 `hover`、`focus-visible`、`disabled` 状态。
- 键盘焦点可见，不要移除默认可访问性提示却不提供替代样式。
- Loading 优先局部化，不要轻易阻塞整个页面。
- 首次加载用 skeleton；局部提交用按钮内 spinner；后台运行用轻量状态提示。
- 错误状态必须说明发生了什么、影响范围、用户下一步能做什么。
- 空状态要解释“这里为什么是空的”以及“用户可以做什么”。

```tsx
// ❌ BAD：只显示没有数据
<div>没有数据</div>

// ✅ GOOD：解释原因并给出动作
<EmptyState
  title="还没有任务"
  description="你可以从首页发起一个新任务，或打开已有会话继续执行。"
  actionLabel="新建任务"
/>
```

## 动画与反馈

- 动画只用于解释状态变化，不用于炫技。
- 页面切换、展开收起、hover 反馈可使用轻量过渡，时长优先 `120ms - 200ms`。
- 长时间动画、弹跳动画、复杂视差都不适合当前产品。
- Toast 只用于短反馈，不承载长说明和关键错误。

## 文案语气

- 默认中文优先，未来支持中英双语时要为国际化留 key，不散落硬编码。
- 文案风格应直接、友好、可行动，避免口号式语言。
- 错误文案要写“发生了什么 + 可以怎么做”。
- 状态文案尽量短而准确，如“任务运行中”“等待你的确认”“任务已停止”。
- 对 Agent 输出的视觉块做清晰区分：思考、步骤、结果、待确认。

```txt
✅ 好：任务已停止，你可以重新运行，或查看上一步日志定位问题。
❌ 差：操作失败，错误码 500。
```

## 可访问性

- 所有表单控件必须有可访问名称，优先使用 `label`、`aria-label`、`aria-describedby`。
- 可点击元素优先使用语义标签 `button`、`a`，不要用 `div` 假装按钮。
- 图标按钮必须有屏幕阅读器可读名称。
- 颜色对比必须满足可读性要求，特别是暗色主题。

## 响应式与桌面适配

- 这是桌面优先产品，但窗口大小仍会变化，所以布局需支持 `1280x720` 起步尺寸。
- 不需要为超窄手机布局设计完整体验，但需保证较窄桌面窗口不崩坏。
- 面板和侧栏在小窗口下可以折叠，但主任务流程必须始终可达。

## 禁止事项

- 不使用无意义插画填充空白。
- 不使用大面积高饱和渐变做主背景。
- 不用多个主按钮竞争注意力。
- 不把关键操作藏进只有 hover 才能发现的位置。
- 不让错误、等待、空状态变成“沉默界面”。
- 不让同一状态在不同页面使用不同颜色和不同文案。

## AI 执行清单

- 新页面先确认它属于 Header、左侧栏、中间 Chat 主区，还是右侧工作台。
- 新组件先确认 `shadcn/ui` 是否已有基础能力可复用。
- 新样式先使用 token 与 Tailwind 语义类，不新增硬编码色值。
- 每个视图都至少考虑 `loading / empty / error / success` 四类状态。
- 每次增加任务流时，同步考虑状态展示、停止入口、失败恢复、用户确认节点和多 Chat 切换体验。
