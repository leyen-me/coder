# PLAN

| 阶段 | 主题 | 主要产出 | 状态 |
| --- | --- | --- | --- |
| 1 | 产品定义与边界 | 目标用户 / 首版 3 个核心场景 / 一句话价值主张 | ✅ 已完成 |
| 2 | 设计规范与代码规范 | 设计规范文档 / 代码规范文档 / AI 可读约束 | ✅ 已完成 |
| 3 | 架构设计 | 应用架构 / 进程模型 / 数据流 / 模块边界 / 存储与扩展架构 | ✅ 已完成 |
| 4 | 本地数据库 | SQLite 数据库 / 本地数据存储结构 | 待开始 |
| 5 | 设置页与配置体系 | 设置页面 / 默认配置 / 用户偏好 / 配置读取机制 / Model Provider 管理 | 待开始 |
| 6 | 主题模式设计 | 暗黑模式 / 亮色模式 / 主题切换机制 | 待开始 |
| 7 | 国际化 | 中文 / 英文 / 语言切换机制 | 待开始 |
| 8 | Agent 核心与首页 | Agent 核心能力 / 首页 / 会话页 / 任务执行页 / 后台任务与可观测性 | 待开始 |
| 9 | 扩展能力模块 | Rules / Skills / MCP / Plugin / WebHook / WebSearch / WebFetch | 待开始 |
| 10 | 桌面工作台能力 | 项目树 / 文件预览与编辑 / 终端窗口 / Browser / Git 分支 | 待开始 |
| 11 | 高级实验能力 | 超长任务 Agent | 待开始 |

## 阶段 1：产品定义与边界

| 项目 | 当前定义 |
| --- | --- |
| 目标用户 | 以电脑为主工作设备的通用知识工作者：学生、内容创作者、运营、助理、行政、独立工作者 |
| 首版核心场景 1 | 对话式任务台：用户用自然语言发起任务，Agent 负责拆解、执行、汇报进度 |
| 首版核心场景 2 | 文件工作台：围绕本地文件进行阅读、总结、整理、改写、生成 |
| 首版核心场景 3 | 研究整理助手：搜索网页信息、提炼结论、输出可交付结果 |
| 一句话价值主张 | 一个小而美的 Desktop Agent，帮普通用户把电脑上的复杂任务变成可交付结果 |

## 阶段 2：设计规范与代码规范

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 建立适合 AI 持续参与开发的统一约束，降低 vibe coding 过程中的发散和返工 |
| 设计规范文档 | 定义信息架构、页面布局、组件规则、视觉语言、交互状态、文案语气 |
| 代码规范文档 | 定义目录结构、命名规则、组件写法、状态管理约定、接口约定、注释与测试要求 |
| 文档要求 | 结构稳定、表达明确、方便 AI 直接读取和执行 |

## 阶段 3：架构设计

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 在进入 Agent 主体开发前，先定义系统架构，避免后续能力叠加时反复推翻 |
| 应用架构 | 明确桌面端应用分层：界面层、应用层、Agent 层、工具层、数据层 |
| 进程模型 | 明确主进程、渲染进程、Agent 执行单元、终端执行单元之间的关系 |
| 数据流设计 | 明确用户输入、上下文组装、任务执行、结果输出、持久化回写的流转路径 |
| 模块边界 | 明确会话模块、任务模块、配置模块、工具模块、工作台模块的职责边界 |
| 存储架构 | 明确 SQLite、本地文件、缓存数据、日志数据分别存什么 |
| 扩展架构 | 为 Rules、Skills、MCP、Plugin、WebHook 预留统一接入位 |
| 设计原则 | 先定边界，再做实现；先保证可维护，再追求能力堆叠 |

### 3.1 应用分层架构

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义桌面应用的整体分层结构，避免界面、业务、执行逻辑互相耦合 |
| 分层建议 | 界面层 / 应用层 / Agent 层 / 工具层 / 数据层 |
| 设计原则 | 上层只依赖下层能力，跨层调用尽量通过统一接口完成 |

### 3.2 进程与执行模型

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义主进程、渲染进程、Agent 执行单元、终端执行单元之间的运行关系 |
| 核心问题 | 哪些逻辑在界面侧，哪些逻辑在后台侧，哪些任务需要独立执行单元 |
| 设计原则 | 保证界面流畅、任务可中断、执行可隔离 |

### 3.3 模块通信机制

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义界面、Agent、工具、数据库、工作台模块之间如何通信 |
| 核心范围 | 页面事件、任务事件、状态更新、工具结果、日志回传 |
| 设计原则 | 通信路径清晰、事件可追踪、避免模块间直接混乱依赖 |

### 3.4 数据与存储架构

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义 SQLite、本地文件、缓存、日志和配置数据的存储边界 |
| 存储对象 | 会话数据、任务数据、消息数据、配置数据、日志数据、产物数据 |
| 设计原则 | 按数据类型分层存储，保证恢复能力、追踪能力和后续扩展能力 |

### 3.5 Agent 执行架构

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义 Agent 从输入、规划、执行、反馈到持久化的完整执行路径 |
| 核心环节 | 输入组装、Planner、上下文管理、工具调用、结果输出、状态更新、持久化回写 |
| 设计原则 | 路径明确、状态一致、失败可恢复、便于后续扩展 Sub-Agent 和 Multi-Agent |

## 阶段 4：本地数据库

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 使用 SQLite 搭建本地数据库，用于存储本软件的用户数据 |
| 数据库类型 | SQLite |
| 存储范围 | 用户在本软件中的业务数据、本地记录、运行过程相关数据 |
| 要求 | 本地优先、结构清晰、方便后续功能持续扩展 |

## 阶段 5：设置页与配置体系

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 先搭好设置页面和配置结构，作为后续功能的统一配置入口 |
| 设置页面 | 提供系统设置、基础开关、配置查看与修改能力 |
| 默认配置 | 定义系统默认配置，作为软件初始运行基础 |
| 用户偏好 | 支持用户保存自己的主题、语言、行为偏好等设置 |
| 配置机制 | 后续 Agent 与系统功能都从统一配置中读取所需参数 |
| Model / Provider 管理 | 提供模型提供商、模型选择、参数配置、默认路由和后续扩展能力 |

## 阶段 6：主题模式设计

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 为系统提供统一的亮色与暗黑主题体验，支持用户自主切换 |
| 亮色模式 | 定义亮色主题下的颜色、层级、组件表现和可读性规则 |
| 暗黑模式 | 定义暗黑主题下的颜色、层级、组件表现和可读性规则 |
| 主题切换机制 | 支持用户在系统内切换主题，并保证界面状态一致 |

## 阶段 7：国际化

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 为系统提供多语言能力，支持用户切换软件语言 |
| 当前语言范围 | 仅支持中文和英文 |
| 语言切换机制 | 支持用户在系统内切换显示语言 |
| 扩展策略 | 其他语言后期再逐步添加 |

## 阶段 8：Agent 核心与首页

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 开始搭建 Agent 核心能力，并完成产品主入口页面 |
| Agent 核心 | 建立任务规划、执行调度、重试恢复、测试执行、子 Agent、多 Agent、上下文管理等核心能力基础 |
| 首页 | 作为产品主入口，承载主要使用流程 |
| 会话页 | 支持用户通过对话发起任务、持续交互、查看上下文 |
| 任务执行页 | 展示任务执行过程、当前状态、结果输出与过程反馈 |

### 8.1 Agent 运行模式

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义 Agent 的最小运行单元与生命周期，确保任务可启动、可观察、可停止、可回收 |
| 运行单元 | 每个 Agent 任务都是独立执行单元，不与主界面共用执行流 |
| 基本原则 | 不强制绑定为一个 OS 线程，但必须支持独立生命周期控制 |
| 生命周期状态 | pending / running / waiting_user / cancelling / cancelled / completed / failed |
| 停止机制 | 用户可主动停止任务，系统进入 cancelling，再根据执行结果进入 cancelled 或 failed |
| 工具执行要求 | 所有接入 Agent 的工具都必须支持取消、中断或超时控制 |
| 界面反馈 | 会话页与任务执行页都要展示当前状态、当前步骤、当前工具和停止结果 |

### 8.2 Agent 状态机

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义 Agent 各状态之间的流转关系，保证执行过程可预测、可追踪、可控制 |
| 初始状态 | pending |
| 运行状态 | running |
| 等待状态 | waiting_user |
| 取消中状态 | cancelling |
| 结束状态 | cancelled / completed / failed |
| 基本流转 | pending -> running -> completed |
| 用户交互流转 | running -> waiting_user -> running |
| 停止流转 | pending 或 running 或 waiting_user -> cancelling -> cancelled |
| 异常流转 | running -> failed |
| 状态要求 | 任一时刻只能处于一个明确状态，所有状态变化都必须可记录、可展示 |

### 8.3 Agent 上下文输入结构

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义 Agent 每次运行时的输入上下文结构，保证 prompt 组装稳定、可复用、可扩展 |
| 用户输入 | 用户当前消息、任务目标、补充说明、选择项 |
| 会话上下文 | 当前会话历史、最近任务记录、上一次 Agent 输出 |
| 文件与附件 | 用户上传或指定的文件、附件、路径引用 |
| Codebase Indexing | 提供代码库索引、符号索引、文件结构和检索结果，作为代码任务的重要上下文来源 |
| 系统配置 | 系统默认配置、用户偏好、当前功能配置 |
| 环境上下文 | 当前语言、当前主题、当前页面、运行模式 |
| 静态 Prompt | 预先定义系统 prompt、角色 prompt、场景 prompt，作为固定约束输入 |
| 动态 Prompt | 根据当前任务、用户输入、文件内容、状态信息实时组装 |
| 结构要求 | 输入结构必须模块化，方便后续新增能力时按块扩展，而不是整体重写 |

### 8.4 Agent 输出结构

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义 Agent 的统一输出结构，确保前端展示、状态追踪、结果存储保持一致 |
| 状态输出 | 返回当前状态、状态说明、状态变更时间 |
| 步骤输出 | 返回当前步骤、步骤列表、已完成步骤、下一步动作 |
| 工具输出 | 返回当前工具、工具执行结果、工具错误信息 |
| 中间结果 | 返回阶段性产物、草稿内容、处理中结果 |
| 最终结果 | 返回最终可交付结果，如文本、文件、摘要、报告、结构化数据 |
| 用户交互输出 | 当需要用户确认或补充信息时，返回问题、选项、等待原因 |
| 错误输出 | 返回错误类型、错误信息、失败阶段、可恢复建议 |
| 结构要求 | 输出结构必须稳定、结构化、可持久化，方便前端渲染和数据库存储 |

### 8.5 工具调用机制

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义 Agent 调用工具的统一机制，保证工具可管理、可中断、可追踪 |
| 工具注册 | 所有工具都需要统一注册，具备名称、用途、输入参数、输出结构、权限要求 |
| 调用方式 | Agent 通过统一调用层发起工具调用，不直接耦合具体工具实现 |
| 调用前检查 | 调用前检查工具是否可用、参数是否完整、权限是否满足 |
| 权限控制 | 涉及敏感操作的工具需要支持用户确认或授权 |
| 执行控制 | 工具调用必须支持运行中、成功、失败、超时、取消等状态 |
| 中断能力 | 工具执行过程中必须支持取消、中断或超时回收 |
| 结果回传 | 工具结果统一回传给 Agent，再由 Agent 输出给前端 |
| 错误处理 | 工具失败时要返回明确错误信息，并支持重试、降级或提示用户 |

### 8.6 持久化与恢复

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 先做持久化能力，不做长期记忆能力，确保 Agent 运行事实可保存、可恢复、可追溯 |
| 设计原则 | 只持久化事实数据、任务状态和执行结果，不引入容易产生幻觉的记忆推断 |
| 持久化内容 | 会话、消息、任务记录、状态变更、工具调用记录、中间结果、最终结果、用户确认记录 |
| 恢复能力 | 应用重启后可恢复会话、任务状态和最近执行上下文 |
| 数据要求 | 数据必须结构化、可追踪、可审计，避免模糊语义记忆污染后续任务 |

### 8.7 危险操作确认机制

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 不做重权限系统，只对高风险操作增加确认机制 |
| 默认策略 | 默认信任 Agent，大部分普通操作直接执行 |
| 需要确认的操作 | 删除文件、批量覆盖、目录结构大改、不可逆命令、系统级高风险操作 |
| 确认触发条件 | 涉及 rm、强制覆盖、批量修改、大结构变化或不可恢复影响时触发 |
| 非目标 | 不做处处弹窗、不做细粒度复杂权限管理 |
| 设计原则 | 只在真正危险的节点打断流程，尽量不影响正常使用流畅度 |

### 8.8 会话模型

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义用户、消息、任务、上下文、结果在会话中的组织关系 |
| 会话定位 | 会话是持续协作空间，不只是聊天记录 |
| 核心关系 | 会话是容器，任务是执行单元，任务挂载在会话之下 |
| 会话内容 | 用户消息、Agent 回复、任务记录、工具过程、中间结果、最终结果、附件引用 |
| 任务关系 | 一个会话内可以连续承载多个相关任务 |
| 上下文延续 | 后续消息默认继承当前会话上下文，并关联最近相关任务结果 |
| 会话结束方式 | 会话默认长期保留，由用户主动新建或切换，不因单个任务完成而自动结束 |
| 设计原则 | 会话负责承载协作历史，任务负责承载执行过程，二者职责分离但相互关联 |

### 8.9 Task Planner

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 把用户目标拆解为可执行步骤、子任务和执行顺序 |
| 核心职责 | 任务理解、步骤拆解、顺序规划、依赖关系管理 |
| 输出内容 | 执行计划、步骤列表、子任务结构、预期结果 |
| 设计原则 | 先做最小可执行规划，不追求过度复杂的长期规划器 |

### 8.10 Terminal Execution

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 为 Agent 提供终端命令执行能力 |
| 核心职责 | 运行命令、读取输出、跟踪状态、停止执行 |
| 设计要求 | 与 Agent 生命周期联动，支持取消、中断、超时控制 |
| 风险控制 | 高风险命令触发危险操作确认机制 |

### 8.11 Retry Loop

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 在任务失败、工具报错或结果不满足要求时，支持有限重试与恢复流程 |
| 触发场景 | 工具失败、测试失败、结果不符合预期、上下文缺失 |
| 核心职责 | 识别失败原因、生成修正动作、控制重试次数、避免死循环 |
| 设计原则 | 重试必须有限、可记录、可中断，不能无限自我循环 |

### 8.12 Test Runner

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 对代码或结果执行验证，作为 Agent 的结果检查能力 |
| 核心职责 | 运行测试、收集结果、提炼结论、反馈给 Agent |
| 输入来源 | 测试命令、验证规则、项目配置 |
| 输出内容 | 测试状态、失败信息、关键日志、是否通过 |

### 8.13 Sub-Agent

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 支持把复杂任务拆给单独子 Agent 执行，再汇总结果 |
| 适用场景 | 主任务过大、任务可拆分、需要隔离上下文时 |
| 核心职责 | 创建子 Agent、分配子任务、回收结果、汇总输出 |
| 设计原则 | 先做可控的单层子 Agent，不先做过深嵌套 |

### 8.14 Multi-Agent

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 支持多个 Agent 并行或分工协作完成更复杂任务 |
| 协作方式 | 并行执行、角色分工、结果汇总 |
| 核心职责 | 任务分发、执行协调、冲突处理、结果聚合 |
| 设计原则 | Multi-Agent 建立在 Sub-Agent 稳定之后，不作为最早实现能力 |

### 8.15 Context 管理

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 统一管理任务上下文、会话上下文、文件上下文、配置上下文和运行环境上下文 |
| 核心职责 | 上下文收集、裁剪、组装、传递、更新 |
| 上下文类型 | 用户输入、会话历史、文件内容、配置参数、环境状态、执行结果 |
| 设计原则 | 优先保证上下文准确和可控，避免无边界堆叠导致噪音和幻觉 |

### 8.16 后台任务与通知

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 支持任务在后台持续运行，并在关键事件发生时通知用户 |
| 核心能力 | 后台运行、任务队列、暂停恢复、完成通知、失败通知 |
| 适用场景 | 长时间任务、无需用户持续盯看的任务、批量任务 |
| 设计原则 | 用户可随时离开当前页面，但仍能感知任务进度和结果 |

### 8.17 可观测性 / Trace / 日志

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 提供 Agent 执行过程的可追踪能力，方便调试、回放和问题定位 |
| 核心能力 | 步骤追踪、工具调用日志、状态流转记录、错误日志、重试记录 |
| 作用 | 帮助理解 Agent 为什么这样做、在哪一步失败、是否需要优化 |
| 设计原则 | 记录事实过程，不制造额外噪音，既便于调试也便于后续产品优化 |

### 阶段 8 实现顺序

| 顺序 | 模块 | 原因 |
| --- | --- | --- |
| 1 | 8.9 Task Planner | 先决定 Agent 如何理解任务和生成执行计划，这是后续执行能力的入口 |
| 2 | 8.15 Context 管理 | 没有稳定上下文，Planner、执行器和重试机制都会漂移 |
| 3 | 8.10 Terminal Execution | 先打通最核心执行能力，形成真正可执行的 Agent 闭环 |
| 4 | 8.11 Retry Loop | 执行能力有了之后，马上补失败恢复，避免 Agent 一次失败就结束 |
| 5 | 8.12 Test Runner | 在执行和重试之后加入验证能力，让 Agent 知道结果是否合格 |
| 6 | 8.13 Sub-Agent | 主体单 Agent 跑稳后，再开始做任务拆分和子 Agent 协作 |
| 7 | 8.14 Multi-Agent | 复杂度最高，必须放在单 Agent 和 Sub-Agent 稳定之后实现 |

## 阶段 9：扩展能力模块

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 为 Agent 提供可配置、可扩展、可集成的能力层 |
| 模块范围 | Rules / Skills / MCP / Plugin / WebHook / WebSearch / WebFetch |
| 设计原则 | 核心能力先稳定，扩展能力后接入；所有扩展模块都应可配置、可禁用、可追踪 |

### 9.1 Rules

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 定义 Agent 在特定场景下必须遵守的固定规则和行为约束 |
| 作用 | 约束输出风格、行为边界、任务偏好、风险控制策略 |
| 特点 | 静态、稳定、优先级高，适合长期约束 |

### 9.2 Skills

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 为 Agent 提供可复用的能力模板和任务处理套路 |
| 作用 | 复用特定任务的 prompt、步骤、工具组合和输出格式 |
| 特点 | 面向任务场景复用，适合沉淀最佳实践 |

### 9.3 MCP

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 接入外部 MCP 能力，扩展 Agent 的工具与资源访问范围 |
| 作用 | 连接外部服务、工具和资源，让 Agent 获得更广泛的执行与读取能力 |
| 特点 | 面向标准协议集成，强调能力接入和统一调用 |

### 9.4 Plugin

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 支持通过自定义插件扩展 Agent 的功能边界 |
| 作用 | 允许系统或开发者按需增加新能力、新工具和新集成方式 |
| 特点 | 面向自定义扩展，强调插件生命周期和可插拔能力 |

### 9.5 WebHook

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 支持系统与外部服务之间的事件通知和自动触发 |
| 作用 | 在任务完成、状态变化、错误发生等事件下向外发送通知或触发流程 |
| 特点 | 面向事件驱动集成，适合自动化联动场景 |

### 9.6 WebSearch

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 为 Agent 提供网页搜索能力，支持发现外部信息来源 |
| 作用 | 帮助 Agent 在研究型任务中查找最新信息、线索和相关网页入口 |
| 特点 | 面向信息发现，不等同于浏览器操作 |

### 9.7 WebFetch

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 为 Agent 提供网页内容抓取与正文读取能力 |
| 作用 | 读取指定网页内容，用于提取正文、分析页面信息和生成结果 |
| 特点 | 面向网页内容获取，不等同于搜索，也不等同于浏览器自动化 |

## 阶段 10：桌面工作台能力

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 扩展软件的工作台能力，让 Agent 在一个小而美的桌面环境中完成项目操作 |
| 产品定位 | 做一个 mini VSCode 风格的工作台，但更聚焦、更轻量、更偏 Agent 协作 |
| 模块范围 | 项目树 / 文件预览与编辑 / 终端窗口 / Browser / Git 分支 |

### 10.1 项目树与文件能力

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 提供项目树、文件预览和文件编辑能力，作为项目操作的基础入口 |
| 核心能力 | 目录浏览、文件打开、文件预览、文件编辑、文件定位 |
| 作用 | 让用户和 Agent 都能围绕项目结构进行操作和协作 |

### 10.2 终端窗口

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 提供统一的终端管理区域 |
| 核心能力 | 终端创建、终端切换、输出查看、运行状态管理、停止执行 |
| 管理范围 | 用户自己运行的终端和 AI 自己运行的终端统一在一个地方管理 |

### 10.3 Browser

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 提供内置浏览器能力，支持 Agent 操作网页 |
| 核心能力 | 页面打开、内容查看、网页交互、浏览过程反馈 |
| 作用 | 支撑网页检索、网页操作、自动化浏览等任务场景 |

### 10.4 Git 分支能力

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 提供分支与 Git 操作能力，支持 Agent 参与项目版本管理 |
| 核心能力 | 分支查看、切换、创建、提交、状态查看、差异查看 |
| 作用 | 让 Agent 能在项目协作中操作代码版本和工作分支 |

## 阶段 11：高级实验能力

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 探索基础能力之外的高级功能和实验性能力，扩展 Agent 的上限 |
| 模块定位 | 不属于首批基础能力，放在主路径之后按需推进 |
| 模块范围 | 超长任务 Agent |

### 11.1 超长任务 Agent

| 项目 | 当前定义 |
| --- | --- |
| 目标 | 支持执行持续时间很长、步骤很多、可跨阶段恢复的任务 |
| 核心能力 | 长时运行、断点恢复、阶段记录、状态跟踪、任务续跑 |
| 作用 | 让 Agent 能处理需要较长时间才能完成的复杂任务 |
| 示例场景 | 例如让 Agent 长时间操作浏览器，或让 Agent 完成某种语言的重实现任务 |
