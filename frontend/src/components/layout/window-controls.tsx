/** Whether native window controls are rendered in the title bar. */
export const HAS_WINDOW_CONTROLS = false;

export function WindowControls() {
  // Window controls are only available in the desktop app.
  return null;
}
